spring:
  security:
    oauth2:
      client:
        registration:
          #카카오
          kakao:
            client-id: cfcb49cda5109c4d0770ebf1fcda7662                # 카카오 클라이언트아이디
            client-secret: 26LlNu83hcgM5c8x9OqXYBjN9EU03GGi        # 카카오 시크릿 키
            client-authentication-method: client_secret_post
            scope:
              - profile_nickname
              - account_email
            client-name: Kakao
            authorization-grant-type: authorization_code
            redirect-uri: http://localhost:8080/login/oauth2/code/kakao
          #네이버
          naver:
            client-id: NTlX9pmcubwkqA8jHYke      # 네이버 클라이언트 아이디
            client-secret: uBP5q_UrJu            # 네이버 시크릿 키
            client-authentication-method: client_secret_post
            client-name: Naver
            authorization-grant-type: authorization_code
            redirect-uri: http://localhost:8080/login/oauth2/code/naver
            scope:
              - name
              - email


        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize  # 로그인 창
            token-uri: https://kauth.kakao.com/oauth/token             # 토큰 받기
            user-info-uri: https://kapi.kakao.com/v2/user/me           # 프로필 정보
            user-name-attribute: id

          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-name-attribute: response

  datasource:
    url: jdbc:mysql://localhost:3306/dotory?createDatabaseIfNotExist=true&serverTimezone=Asia/Seoul   # 개인 local DB, dotory 사용합니다.
    username: root                         # 개인 DB 계정 root 입니다.
    password: 1234                         # 개인 DB 비밀번호 1234 입니다.
    driver-class-name: com.mysql.cj.jdbc.Driver

  # Spring Starter Mail (구글)
  mail:
    host: smtp.gmail.com
    port: 587
    username: setilvereun@gmail.com         # 서은비 구글 계정입니다.
    password: qwjequkxxkqctiey              # 구글에 생성한 dotory 앱 비밀번호 입니다.
    protocol: smtp
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
        default_batch_fetch_size: 10
    open-in-view: false

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace

cloud:
  aws:
    enabled: false


jwt:
  secret: "eunbiJwtSecretKey1234567890123456!"               # jwt 시크릿 키
  expiration: 86400000